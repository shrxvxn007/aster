# Build output
build/
out/
*.o
*.obj
*.a
*.so
*.dylib
*.exe

# CMake scratch / CTest artifacts (Testing/Temporary/CTestCostData.txt etc.)
Testing/
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile
compile_commands.json
.deps/
.derivatives/

# Profiling / coverage
*.gcov
*.gcda
*.gcno
callgrind.out.*
massif.out.*

# Editor / IDE
.vscode/
.idea/
*.swp
*~
*.swo
*.tmp
*.bak

# Optional local data — large / private ITCH files should not live in the
# repo, but a tiny deterministic sample under samples/ is committed so the
# `aster_replay --itch-file samples/test.itch` command works out of the box.
*.itch
!samples/*.itch
*.pcap
data/

# macOS
.DS_Store
*.dSYM/

# CLion / IDE build directories
cmake-build-debug/
cmake-build-release/
